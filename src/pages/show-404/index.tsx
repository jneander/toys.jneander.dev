import {PrimaryLayout} from '../../shared/components'
import {renderPage} from '../render-page'

renderPage(() => (
  <PrimaryLayout>
    <main className="flow">
      <h1>Page Not Found</h1>
    </main>
  </PrimaryLayout>
))
